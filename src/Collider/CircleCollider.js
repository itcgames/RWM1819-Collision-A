class CircleCollider extends Collider
{
    /**
     * The default constructor of the class.
     * @param {Circle} circle 
     */
    constructor(circle) {
        super(circle)  //  Call the parent classes constructor.
    }
}