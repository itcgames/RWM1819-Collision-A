class PolygonCollider extends Collider
{
    /**
     * The default constructor of the class.
     * @param {Polygon} polygon 
     */
    constructor(polygon) {
        super(polygon)  //  Call the parent classes constructor.
    }
}